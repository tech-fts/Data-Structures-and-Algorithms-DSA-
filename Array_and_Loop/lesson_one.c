#include <stdio.h>
#define size 5

void user_input(){
    int numbers;
    printf("Please enter number that you want to add:");
    scanf("%d", &numbers);
}

int main(){
    int numbers[size];
    return 0;
}